services:
  flask:
    container_name: flask
    build: ./flask
    volumes:
      - ./flask/app:/flask/app
    networks:
      - web-network
  
  nginx:
    container_name: nginx
    build: ./nginx
    ports:
      - '80:80'
    depends_on:
      - flask
    networks:
      - web-network

networks:
  web-network:
